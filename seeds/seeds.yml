version: 2

seeds:
  - name: medicare_state_fips
    config:
      database:  "{{ var('output_database') }}"
      schema:  "{{ var('output_schema')}}"
      tags: terminology
      column_types:
        fips_code: varchar(4)